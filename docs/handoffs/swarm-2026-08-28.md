# Swarm run — 2026-08-28 (agent-loop perf hub #648)

**Ledger + briefing.** This file is both the dispatch roster (at-most-once
semantics live here, not in the orchestrator's memory) and the knowledge-bank
briefing a `relief` session would read. Update on **every** dispatch, report,
merge, and owner call. Delete when the run is closed out.

Scope fence: **#649, #650, #651, #652 only.** #654/#655/#658/#659 are
`Needs design` — not this run's.

## Roster

| Unit | Issues | Brief | Drone | State |
|---|---|---|---|---|
| step-0 smoke test (#652 gate) | #652 | — (orchestrator, in-context) | — | **GREEN@19:47**, posted to #652, hook reverted |
| prose cluster | #649, #651, #650 | `scratchpad/brief-prose.md` | `prose` | `dispatched@19:52` |
| ctx-hook | #652 | `scratchpad/brief-ctxhook.md` | `ctxhook` | `dispatched@19:52` |

## Decisions not yet persisted elsewhere

- **File-overlap fact (owner, 2026-08-28):** #649/#650/#651 all touch
  `swarm`/`drone`/`handoff` SKILL.md. They are ONE drone, never parallel.
  #652 is file-disjoint (`.claude/settings.json` + a script).
- **#652 step-0: GREEN.** Injected `additionalContext` DOES reach a subagent's
  model (haiku subagent quoted the marker verbatim, as a `<system-reminder>`).
  #652 is viable and is being implemented. → persisted as a #652 comment.
- **#652's swarmify comment was WRONG about `transcript_path`** — a subagent's
  payload carries the PARENT's `session_id` and `transcript_path`; the subagent
  is identified only by `agent_id`. Its own transcript is at
  `dirname(transcript_path)/<session_id>/subagents/agent-<agent_id>.jsonl`.
  Latch must key on `session_id`+`agent_id`. → persisted as a #652 comment.
- **#649 acceptance 4** (follow-up measurement one week after landing) is
  **not landable in this run** — it is a deferred measurement pass. The issue
  cannot fully close on merge; leave that bullet as the open tail.

## Harness observations from this run (for the skill's unsettled idle section)

- The `swarm` skill records the file-pointer dispatch shape as avoiding the
  teammate idle, 2/2. **This run contradicts that, 2/2 the other way**: both
  `prose` and `ctxhook` were spawned with a one-line prompt pointing at a brief
  file and still returned "will receive instructions via mailbox". So the
  file-pointer shape does **not** remove the round-trip — the `SendMessage`
  pointer was still required. Fold into #595 rather than the skill's claim.
- `advisor` IS already forbidden in `drone/SKILL.md:111`. No prose fix needed.

## Next steps

1. Collect `prose` and `ctxhook`. Review diffs; ownership check first.
2. Batch merge: rebase each, ff. **Do NOT run `mise run test` on this train** —
   it is markdown + a hook script + `.claude/settings.json`, zero `.gd`/`.tscn`,
   so the 162s suite cannot observe any of it. Running it would be the exact
   reflex #649 exists to delete, in the run that lands #649. The real gates for
   THIS train are `python3 -m json.tool .claude/settings.json` and the ctxhook
   drone's own script check.
2b. **Before ff'ing ctxhook**, run its script by hand over the malformed /
   missing / truncated / no-usage payload cases and confirm empty stdout +
   exit 0. Fast-forwarding arms the hook on this machine immediately (checked-in
   settings, hooks reload live) and the owner has a parallel Sonnet TUI — a
   traceback on stdout breaks the JSON contract for every agent here, and it
   fails on THEIR next tool call, not mine.
3. Orchestrator-owned acceptance left over after merge:
   - #652 acc. 2 — verify silence under 150k on a real live session (needs the
     hook installed in the main checkout, i.e. post-merge).
   - **#652 real-drone check (blocks calling #652 done).** Step-0 proved
     injection reaches a subagent; it did NOT prove the reported number is the
     *subagent's*. Acc. 2 as written is trivially satisfied and proves nothing
     about the drone path. Post-merge: temporarily drop the first threshold to
     ~20k, spawn a haiku subagent, confirm the line fires IN the subagent and
     the number tracks the subagent's usage, not the (much larger) parent's.
     Also confirms `subagents/agent-*.jsonl` is written live during a run rather
     than flushed at exit.
   - #652 acc. 5 / #649 acc. 4 — future observation passes, cannot land today.
     Leave both issue tails open; do not claim them closed.
4. Push is owner's call — `master` is ahead 1 with an unrelated commit.
