# Handoff — 2026-09-02 architecture review, halted mid-fix (token window)

Six read-only Opus reviewers audited 2026-08-18..09-02 (663 commits, ~150 closed issues) for
architectural drift; six fixers were dispatched in worktrees and **stopped mid-flight** at 15:34
when the lead's context ran out. Their commits are on their branches. Nothing is merged.

Verdicts: combat **minor**, network **minor**, UI **minor**, spells **real**, stats **real**,
corpus **real**. The flows are clean (only a rebuilt `AttackRecord` mutates the world; no refs on
the wire; one `@rpc`; one transition path; no `get_tree().paused` outside PauseMenu); the drift is
mirrors that swarms shipped + prose that lagged the code.

Baseline on master `cb6b526`: **2 failing / 3502 passed / 386 scripts** — both failures are
`test_lightning_bolt_vfx.gd` and are caused by the owner's UNCOMMITTED editor WIP on
`ui/vfx/projectile/visual/lightning_bolt_body.tscn` (pins `emissive_tier_start/end = 3.0`, kills
the #686 ramp, hand-picks `modulate = 2.45` against the hdr-color rule). Committed HEAD is green.
Leave that WIP alone — but note the test pins an authored `.tscn` value (owner-tunes anti-pattern).

## Branches to merge (all off master cb6b526; fixer brief: `review-2026-09-02-fixer-brief.md`)

| branch | unit | commits | landed | NOT landed (finding → what) |
|---|---|---|---|---|
| `worktree-agent-a2eeb74845de01345` | net (Opus) | 6 | CommandLink reads `transport.local_peer_id()`; MetaRoot stamps from Wire; `_offers_ai_opponents = not _is_client()`; loot_offer doc; harness rule paths; unpinned max_hops | F7: `battle_system.gd:263-272` `_seed_source` comment still promises the thing #457 ruled against (rewrite citing #457's closing comment) |
| `worktree-agent-a33a1d109993d2d6f` | spells (Opus) | 3 | one `GhostLoopBody`; picker `set_caster()`; tooltip shows `max_hops` raw | F2: drain-quartet dedupe into `ComposedProjectileVisual` (`forward_tint_to_body` / `forward_context_to_companions`; delete `HealingBeamOrbVisual`, shrink `ReverberatorArrivalVisual`; restores `arc_weight` for both) — was mid-read; F4: `spell-gating-tuning.md` → 3 `needs-design` issues with verbatim owner quotes; F5: dead `class Beat` + TODOs → #722; F6: `highlight_provider.gd:27` stale comment |
| `worktree-agent-a505e38aa79c3e56a` | stats (Opus) | 3 | `DistanceScale.uses_bound()`; `TagAuraEffect` composes `AuraEffect`'s walk; stats-system rule rewritten for #660 | F4: `effect-system.md` three "not supported" lies + batching section (was mid-edit, check the worktree's dirty state); F5: `stat_pool.gd:62-64` + `procgen-v4.md:72` refund clause; F6: TODO residue |
| `worktree-agent-abaec39ca6515899f` | combat (Sonnet) | 3 | `RangedDamageFormula` timing consts deleted; ranged-fixtures hold section cut; attack-timeline ramp section rewritten | F4: `entity-death.md` cascade address → `EntityCombat.apply_cascade` (mid-edit); F5: `loot_system.gd:62-64` pick_registry doc; F6: `dealloc_entry.gd:69-75` #520 note |
| `worktree-agent-ac782dc4c0ab22e6f` | ui (Sonnet) | 4 | StatBoardOverlay fallback gone; CalloutBand tints from StatRegistry; TODOs → #723; test_settings_row docstring | (check whether `ui-palette.md` `paths:` was widened — F2 second half) |
| `worktree-agent-a6f288d343743194b` | corpus (Sonnet) | 2 | CLAUDE.md `Wire` row + BattleSystem one-liner; FOCUS pruned | F4: delete spent `presentation-clock-and-381.md`, `lan-wave-0.md` (move its owner quote into `multiplayer-sync-model.md:~704` first), fix dead links in `codebase-audit-2026-08-13.md:3` + `domain-doc-alignment-2026-08-23.md`; F5: `testing.md` 110s→184.6s/386 scripts + once-per-unit cap, CLAUDE.md figures; F6: 12 dead path refs (table in the fixer prompt / rev-hygiene report) |

Merge order: any; expected overlap only in docs. Per branch: `mise run check`, the touched
`test:dir`; one full suite on the merged result (read run-health + script count, not just the number).
Then `git worktree remove` each `.claude/worktrees/agent-*` and delete this file + the two briefs.

## Filed this session
#720 CoreAura/HealAura → AuraEffect port (second aura system; can't run on a shadow world) ·
#721 LobbyScreen last code-composed screen · #722 MagicBounceCoordinator decomposition ·
#723 CombatReadoutCard TODOs · **#724 owner call: does a peer re-derive procgen or receive the
snapshot?** (dormant-core rule vs lint-transcendentals exemption; decides the `pow` in
`stat_pool.gd:228`) · #725 stats-system.md 724-line doc-in-a-rule.

## Not fixed, needs owner
- `docs/handoffs/audit-2026-08-13/` — ~200K tracked, index `TRIAGE.md` never committed. Restore or file+delete.
- #668 confirmed live (mirror-side loot picker has no collector-ownership filter; wrong at 3 peers).
- `mise run rules-hygiene`: 16 violations, all #363's known ledger, none new.
