# Handoff — 2026-09-02 architecture review, residue

Six read-only Opus reviewers audited 2026-08-18..09-02 (663 commits, ~150 closed issues) for
architectural drift. Verdicts: combat/network/UI **minor**, spells/stats/corpus **real**. The
flows are clean (only a rebuilt `AttackRecord` mutates the world; no refs on the wire; one `@rpc`;
one transition path; no `get_tree().paused` outside PauseMenu); the drift was mirrors that swarms
shipped + prose that lagged the code. 33 fix commits merged to master 2026-09-02 (`cb6b526..`),
suite 387 scripts / 3505 passed / 2 failing — both failures are the owner's uncommitted editor WIP
on `ui/vfx/projectile/visual/lightning_bolt_body.tscn` (pins `emissive_tier_start/end = 3.0`, kills
the #686 ramp, hand-picks `modulate = 2.45` against the hdr-color rule). Note the test pins an
authored `.tscn` value — the owner-tunes anti-pattern; re-point it at a hand-built `BoltBody`.

## Findings verified but NOT landed (fixers were stopped mid-flight)

- **spells F2 (M, the one that matters):** `ComposedProjectileVisual`'s drain quartet (`_forward` /
  `_track` / `_on_child_finished` / `_check_done` + deferred `finished`) is copied into
  `healing_beam_orb_visual.gd`, `reverberator_arrival_visual.gd`, `resonator_edge_visual.gd`,
  `trail_blazer_edge_visual.gd`, `ghost_loop_body.gd`. The two full forks each exist for ONE bool
  the wrapper already models (`forward_crit_to_body`): add `forward_tint_to_body` /
  `forward_context_to_companions`, delete `HealingBeamOrbVisual`, shrink `ReverberatorArrivalVisual`
  to the `visit_index` ring ramp. **Neither fork forwards `arc_weight` (#708)** — the channel is
  dead for those two spells today.
- **spells F4:** `docs/handoffs/spell-gating-tuning.md` is the SOLE home of an owner-approved
  gating table (§1), a "do not reopen" ruling (§1b/§3) and a HARD CONSTRAINT — none filed, none
  implemented (Cyclone min_degree 4 vs table 5, Reverberator 3 vs 6, Leafblower 2 vs 4, Resonator
  2 vs 4, Healing Beam mana 5 vs 4; no `spell_hops` stat; `hop_range_finder.gd:35` still multiplies).
  Open one `needs-design` issue per section with the owner quotes verbatim + dated; move §3's
  ruling into the domain doc; shrink the handoff to pointers. Owner tunes the table.
- spells F5: `magic_bounce_coordinator.gd:101-110` dead `class Beat` + two TODOs → point at #722.
- spells F6: `ui/highlight/highlight_provider.gd:27` says PROPAGATION is "not yet driven"; #679 drives it.
- net F7: `systems/battle_system.gd:263-272` `_seed_source` comment still promises the change #457
  ruled AGAINST (closing comment 2026-08-22: stays self-randomised, run seed is procgen's alone).
- stats F5: `procgen/pools/stat_pool.gd:62-64` + `docs/domain/procgen-v4.md:72` still state the
  #637-retired refund economics. stats F6: unfiled TODOs at `stat_pool.gd:240,249`,
  `graph_procgen.gd:1169,1204`, `effect_context.gd:12-14`.
- combat F5/F6: `systems/loot_system.gd:62-64` pick_registry doc ("nothing reports today" — #564
  does); `combat/dealloc_entry.gd:69-75` #520 note reads as current.
- ui F2b: widen `.claude/rules/ui-palette.md` `paths:` to cover `ui/announcement_layer/**` (check
  whether the merged CalloutBand commit did it).
- corpus F5: `.claude/rules/testing.md` says ~110s; measured 184.5s / 387 scripts / 3507 tests
  (2026-09-02) — update it and CLAUDE.md's figures, add the once-per-unit cap. corpus F6: dead
  path refs in `attack_plan_system.md` (`attack/formulas/magic_damage.gd`, `melee_damage.gd`),
  `game-session.md` (`ui/menu/lobby_screen.gd` → `ui/frontmatter/panels/`), `procgen-v2.md`
  (`procgen/tags.tres` → `procgen/archetypes/tags.tres`; `tags_constants.gd` gone),
  `sandbox-framework.md` (`allocation_vfx_showcase.tscn` gone), `scene-composition.md`
  (`skill_node_addon.tscn` gone), `skillnode-emblem.md` (`core_marker.gd` gone),
  `spell-propagation.md` (`test_all_neighbours_propagation.gd` → `test_propagation.gd`?),
  `stat-knobs-and-bins.md` + `stats_panel_refactor.md` + `.claude/skills/manage-stats/SKILL.md`
  (`ui/stats_panel.gd` → `ui/tooltip_fan/panels/node_stats_panel.gd`), `melee-blade-sim.md`
  (`attack/melee/HANDOFF_OPUS.md` gone).

## Filed
#720 CoreAura/HealAura → AuraEffect (second aura system; can't run on a shadow world) · #721
LobbyScreen last code-composed screen · #722 MagicBounceCoordinator decomposition · #723
CombatReadoutCard TODOs · **#724 owner call: peer re-derives procgen or receives the snapshot?**
(decides the `pow` in `stat_pool.gd:228`) · #725 stats-system.md doc-in-a-rule.

## Owner
`docs/handoffs/audit-2026-08-13/` — ~200K tracked, its `TRIAGE.md` was never committed: restore
or file+delete. #668 confirmed live (mirror-side loot pick has no collector filter; wrong at 3
peers). `rules-hygiene`: 16 violations, all #363's ledger, none new.
