# Handoff — 2026-09-02 architecture review, residue

Six read-only reviewers audited 2026-08-18..09-02 (663 commits, ~150 closed issues) for
architectural drift. Verdict: mostly clean (only a rebuilt `AttackRecord` mutates the world; no
node/entity refs on the wire; one `@rpc`; one transition path; no `get_tree().paused` outside
PauseMenu). The drift that existed was mirrors that swarms shipped (duplicate implementations of
one contract) and prose that lagged the code. 33 fix commits already merged to master
(`cb6b526..a713a73`). Suite: 387 scripts / 3505 passed / 2 failing — both failures are the owner's
**uncommitted editor WIP** on `ui/vfx/projectile/visual/lightning_bolt_body.tscn`; not caused by
this review, not fixed by it (see "Known pre-existing failure" below).

## To resume: pick any item below and fix it directly — no other context needed

Each item names the file, what's wrong, and what to do. None depend on each other.

1. **Spell VFX: two forks silently drop `arc_weight`.** `ComposedProjectileVisual`'s drain
   machinery (`_forward` / `_track` / `_on_child_finished` / `_check_done` + a deferred `finished`
   signal, in `ui/vfx/projectile/visual/composed_projectile_visual.gd`) is copied wholesale into
   `healing_beam_orb_visual.gd`, `reverberator_arrival_visual.gd`, `resonator_edge_visual.gd`,
   `trail_blazer_edge_visual.gd`, `ghost_loop_body.gd`. The two full forks (`HealingBeamOrbVisual`,
   `ReverberatorArrivalVisual`) each exist for exactly one behavioural difference the wrapper
   already models via a bool (`forward_crit_to_body`): Healing Beam doesn't want tint forwarded,
   Reverberator doesn't want `_on_context` forwarded to companions. **Neither fork forwards
   `arc_weight` (#708) — that channel is dead for those two spells right now.**
   Fix: add `forward_tint_to_body` / `forward_context_to_companions` exports to
   `ComposedProjectileVisual`; delete `HealingBeamOrbVisual` (repoint its scene at the wrapper with
   those exports set); shrink `ReverberatorArrivalVisual` to just the `visit_index` ring ramp.
   This is the highest-value item on this list — probably a half-day of real refactor.

2. **A handoff file is the only record of three owner rulings on spell balance, and none of them
   match the code.** `docs/handoffs/spell-gating-tuning.md` holds an owner-approved gating table
   (§1), a "do not reopen" ruling (§1b/§3), and a hard constraint — but per `.claude/rules/handoffs.md`
   a handoff is only supposed to *point at* a decision's real home (an issue/doc/rule), never *be*
   it. Checked against `attack/spell/defs/*.tres`: Cyclone `min_degree=4` vs table says 5,
   Reverberator `3` vs table `6`, Leafblower `2` vs `4`, Resonator `2` vs `4`, Healing Beam mana
   `5` vs `4`; no `spell_hops` stat exists; `attack/range_finder/hop_range_finder.gd:35` still
   multiplies the way §3 says it shouldn't.
   Fix: open one GitHub issue per section, quote the owner verbatim with the date, label
   `needs-design` (the owner tunes the actual numbers, don't just apply the table yourself). Move
   §3's ruling into a domain doc. Then shrink the handoff to pointers at the new issues.

3. **Dead code + stale comment, `ui/vfx/coordinator/magic_bounce_coordinator.gd:101-110`.** An
   unused inner `class Beat` (zero references anywhere) plus two unfiled TODOs. Delete the class;
   point the TODOs at issue #722 (already filed, covers this exact decomposition).

4. **Stale comment, `ui/highlight/highlight_provider.gd:27`.** Says the PROPAGATION highlight role
   is "not yet driven — see issue." It's been driven by `MagicAttackPlan` since #679. Update the
   comment to say so.

5. **Stale comment, `systems/battle_system.gd:263-272`.** The `_seed_source` docstring still
   promises to thread `RunConfig.seed` into `BattleSystem` "per #457" — but #457's closing comment
   (2026-08-22) ruled the opposite: stays self-randomised, the run seed belongs to procgen alone.
   Rewrite the comment to state the settled position instead of a stale TODO.

6. **Stale doc claim, `procgen/pools/stat_pool.gd:62-64` and `docs/domain/procgen-v4.md:72`.**
   Both still describe the debuff-pool refund economics that #637 retired. Delete/update those
   passages.

7. **Unfiled TODOs.** `procgen/pools/stat_pool.gd:240,249`, `procgen/graph_procgen.gd:1169,1204`,
   `effects/effect_context.gd:12-14`. Read each, either file a real issue or delete if it's stale
   musing.

8. **Stale doc, `systems/loot_system.gd:62-64`.** Says nothing consumes the remote-collector case
   ("nothing reports today") — #564 already wired a consumer. Update the comment.

9. **Stale doc, `combat/dealloc_entry.gd:69-75`.** Its note about #520 not yet being done reads
   like #520 is still open; #520 has landed. Update or remove the note.

10. **Rule scope gap.** `.claude/rules/ui-palette.md`'s `paths:` glob may not cover
    `ui/announcement_layer/**` (worth double-checking whether the CalloutBand fix already widened
    it — grep the rule file first before doing anything).

11. **Doc says the test suite is faster than it is.** `.claude/rules/testing.md` says "~110s"; the
    real number as of 2026-09-02 is 184.5s / 387 scripts / 3505 tests. Update it, and add a
    one-line cap ("run the full suite at most once per unit of work") since CLAUDE.md already
    states that policy elsewhere and this rule doesn't repeat it.

12. **Dead path references** — each of these docs points at a file/class that no longer exists;
    repoint or delete the sentence:
    - `docs/domain/attack_plan_system.md` → `attack/formulas/magic_damage.gd`, `melee_damage.gd` (gone)
    - `docs/domain/game-session.md` → `ui/menu/lobby_screen.gd` (moved to `ui/frontmatter/panels/`)
    - `docs/domain/procgen-v2.md` → `procgen/tags.tres` (moved to `procgen/archetypes/tags.tres`); `tags_constants.gd` (gone)
    - `docs/domain/sandbox-framework.md` → `allocation_vfx_showcase.tscn` (gone)
    - `docs/domain/scene-composition.md` → `skill_node_addon.tscn` (gone)
    - `docs/domain/skillnode-emblem.md` → `core_marker.gd` (gone)
    - `docs/domain/spell-propagation.md` → `test_all_neighbours_propagation.gd` (probably renamed `test_propagation.gd` — verify)
    - `docs/domain/stat-knobs-and-bins.md`, `docs/domain/stats_panel_refactor.md`, `.claude/skills/manage-stats/SKILL.md` → `ui/stats_panel.gd` (moved to `ui/tooltip_fan/panels/node_stats_panel.gd`)
    - `docs/domain/melee-blade-sim.md` → `attack/melee/HANDOFF_OPUS.md` (gone)

## Already filed as GitHub issues (nothing to do here, just context)
#720 CoreAura/HealAura → AuraEffect (a second aura system that can't run on a shadow world) ·
#721 LobbyScreen is the last code-composed screen · #722 MagicBounceCoordinator decomposition
(covers item 3 above) · #723 CombatReadoutCard TODOs · **#724 needs an owner decision: does a
peer re-derive procgen from the seed, or receive a transmitted snapshot?** (a rule and a lint
disagree; it decides whether `stat_pool.gd:228`'s `pow(cost, 0.5)` is safe) · #725 `stats-system.md`
is a 724-line rule that should mostly move to `docs/domain/`.

## Needs an owner decision, not filed
- `docs/handoffs/audit-2026-08-13/` — ~200KB of tracked audit findings whose index file
  (`TRIAGE.md`) was never committed. Either restore the index or turn the surviving findings into
  issues and delete the directory.
- Issue #668 (open already) confirmed still live: the mirror-side loot picker has no
  collector-ownership filter — harmless at 2 players, wrong at 3+.

## Known pre-existing failure (not part of this review, don't try to fix it here)
The 2 failing tests in `test/unit/vfx/test_lightning_bolt_vfx.gd` come from your own **uncommitted**
editor changes to `ui/vfx/projectile/visual/lightning_bolt_body.tscn` (`git status` will show it
dirty). That WIP pins `emissive_tier_start = emissive_tier_end = 3.0`, which kills the per-hop
dimming ramp from #686, and hand-picks `modulate = 2.45` instead of a named `Emissive` tier (the
hdr-color rule wants named tiers only). Either finish the tuning and update the two tests to match
your new numbers, or `git checkout` the file to get back to a green suite.

## Verifying the merge itself (if you want a second pass)

Three worktrees had **uncommitted** WIP when the fixers were stopped mid-task; I read each diff,
judged it complete, and applied it by hand. All three touched `.md` only (`entity-death.md`,
`effect-system.md`, the handoffs + `multiplayer-sync-model.md` set) — no code risk there.

The 12 commits that touch `.gd`/`.tscn`/`.tres` (`git log cb6b526..a713a73 -- '*.gd' '*.tscn'
'*.tres'`) were committed by the fixers themselves before they were stopped — verified post-merge:
no orphaned references to renamed/deleted files, `.uid` sidecars present, `mise run check` clean,
full suite green (387 scripts / 3505 passed, same 2 pre-existing unrelated failures as baseline).
That's structural evidence, not a semantic review of paths the suite doesn't cover — in particular
nobody has independently re-read `network/command_link.gd`'s peer-id change, the `TagAuraEffect`
refactor, or `DistanceScale.uses_bound()` against their callers a second time.

Because the fixers were stopped with `TaskStop` rather than finishing, none of them wrote the
structured "Landed / Skipped / Merge notes" report their brief asked for — only a one-line
fragment of whatever they were mid-typing survives in the kill notification. The full raw
transcript of every reviewer and fixer session **does** survive (durable, not `/tmp` — those are
symlinks into it) at:

```
~/.claude/projects/-home-bramh-skill-tree-of-life/5ae908d7-dd5d-5337-b76f-89edaef6b7ad/subagents/agent-<id>.jsonl
```

Fixer agent ids (code-touching units first):
- `a33a1d109993d2d6f` — spells fixer (GhostLoopBody rename, tooltip, picker bar)
- `a505e38aa79c3e56a` — stats fixer (TagAuraEffect, DistanceScale.uses_bound)
- `a2eeb74845de01345` — network fixer (CommandLink peer-id, MetaRoot stamp, lobby)
- `abaec39ca6515899f` — combat fixer (RangedDamageFormula deletion)
- `ac782dc4c0ab22e6f` — UI fixer (StatBoardOverlay, CalloutBand)
- `a6f288d343743194b` — corpus fixer (docs only)

A cheap model can be pointed at one `.jsonl` and asked "did this agent flag anything about
`<file>` it didn't act on, or second-guess a finding from its brief?" — that recovers the missing
self-report. Same trick works on the six reviewer transcripts if a finding above turns out wrong.
