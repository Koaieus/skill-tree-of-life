class_name FoldTerms
extends RefCounted

## A resolved modifier pipeline — plain numbers, no boards, no stats.
## Produced by [method ModifierBins.resolve]; stub, see describe.

var add: float = 0.0
var inc: float = 0.0
var bon: float = 0.0
var mult: float = 1.0
## The SET winner's effective value, or null when no SET applies.
var set_value: Variant = null


func describe(_base_label: String = "X") -> String:
	return ""
