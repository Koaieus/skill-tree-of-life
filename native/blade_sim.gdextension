; The blade solver GDExtension (#798) — see docs/domain/melee-blade-sim.md.
;
; The binaries are NOT committed; build them with `mise run native:build`
; (host platform) or `-- <target> windows` to cross-compile, or fetch them
; with `mise run native:fetch`. A missing binary is a broken checkout since
; #847 (BladeSim push_errors on every swing; `mise run check` still passes),
; and the EXPORTER hard-fails on a key declared here whose file is absent, so
; `mise run build` refuses up front for any platform it is asked to export
; (#806).
;
; Declare nothing you do not build (#844): the matrix is exactly linux +
; windows x86_64, debug and release. linux.arm64 and macos.* are removed on
; purpose — re-add a target the day someone needs it, WITH its binary, never
; ahead of one.

[configuration]

entry_symbol = "blade_sim_library_init"
compatibility_minimum = "4.7"
reloadable = false

[libraries]

linux.debug.x86_64 = "res://native/bin/libbladesim.linux.template_debug.x86_64.so"
linux.release.x86_64 = "res://native/bin/libbladesim.linux.template_release.x86_64.so"
windows.debug.x86_64 = "res://native/bin/libbladesim.windows.template_debug.x86_64.dll"
windows.release.x86_64 = "res://native/bin/libbladesim.windows.template_release.x86_64.dll"
