A test asserts the formula (`test/unit/`), the composed scene or the real clock (`test/integration/`, 15 s/script), or two processes (`mp:e2e`); the schedule is the unit's output and the clock a consumer, a wait is budgeted in seconds never ticks, and an arrange-step write into another unit's internals is a seam to expose, not a fixture. See docs/domain/testing-tiers.md
