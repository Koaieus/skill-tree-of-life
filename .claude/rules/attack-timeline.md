An attack RESOLVES on a shadow world and lands there as it goes — so a kill on wave N gates wave N+1 — and the authority then replays its own recorded result on the live world exactly as a peer does; nothing but a rebuilt `AttackRecord` ever mutates the real world. See docs/domain/attack-timeline.md
