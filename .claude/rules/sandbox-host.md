The in-editor **sandbox host** (`addons/sandbox_host/`) unifies dev-tooling surfaces as tabs; a live tab is an inherited scene of `sandbox_live_tab.tscn` that instances its panel scene under `%PanelHost` (copy `tabs/70_bloom_tab.tscn`) — never code-composed or injected via the legacy `panel_scene` export. See docs/domain/sandbox-framework.md
