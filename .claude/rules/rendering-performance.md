A level renders a few hundred SkillNodes + Edges at once; there the fragment shader is free and the **draw call** is not — batching breaks on a different texture *or material*, so per-instance variation goes in `modulate`/UV/`INSTANCE_CUSTOM`, never per-node uniforms. See docs/domain/rendering-performance.md
