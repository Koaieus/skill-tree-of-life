A level renders a few hundred SkillNodes + Edges at once, so judge any per-node visual at that scale; there the fragment shader is free and the **draw call** is not — batching breaks on a different texture *or a different material*, so per-instance variation belongs in `modulate`/UV/`INSTANCE_CUSTOM`, never in per-node uniforms. See docs/domain/rendering-performance.md
