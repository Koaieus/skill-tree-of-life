`mise run refresh` is a headless editor pass with a verdict — never hand-diff editor churn; the doc indexes the symptoms that route to it (stale class cache, orphan `.uid`, a parse error `check` missed, anything needing a rendered frame). See docs/domain/godot-workflow.md
