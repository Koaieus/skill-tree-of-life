About to hardcode a tuning rate, bolt an extra bucket onto a pool, or write `base_value` to force a stat to an exact number? Each has a house answer (a board stat as formula input, a clamping-setter bin, one door onto a pool's cap, a `SET` modifier for a derived stat, an overlay bin for a resource's own number) — pick from the doc, never improvise. See docs/domain/stat-knobs-and-bins.md
