Launch a long command (the suite, `refresh`, a bench) ONCE with `run_in_background: true`, then END YOUR TURN — no sleep, no tail, no re-reading the output file; the harness resumes you on exit, so every poll is a round-trip bought for nothing. See docs/domain/long-running-commands.md
