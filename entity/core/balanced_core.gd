@tool
class_name BalancedCore
extends CoreClass

## The vanilla class — adds +10 STR / DEX / INT and nothing else.
## Serves as the baseline against which other classes are tuned: every
## class delta is "vs Balanced". Modifier set lives in balanced_core.tres.
