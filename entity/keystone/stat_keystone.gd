@tool
class_name StatKeystone
extends Keystone

## Concrete [Keystone] that's nothing but a hand-crafted modifier set. Uses
## the base [Keystone] apply/remove behaviour as-is — exists primarily so
## designers can pick a concrete `.tres` script without overriding anything.
